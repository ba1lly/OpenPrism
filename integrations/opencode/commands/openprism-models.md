---
description: List the models available to Prism's current backend
---

Use the `openprism_models` tool and show me the available `provider/model` refs
(connected providers first). These are the refs I can pass as the panel for
`/openprism` and `/openprism-code`, or as the two models for `/openprism-bakeoff`.
