---
description: Multi-model coding panel — best-of-N candidates, judge selects + repairs
---

Use the `openprism_code` tool with `task` set to the following, then present the
final solution and its "Verify by:" line. The judge has already selected and
repaired the strongest candidate — relay it rather than re-solving:

$ARGUMENTS
