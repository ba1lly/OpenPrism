{
  "mcpServers": {
    "openprism": {
      "command": "uvx",
      "args": ["--from", "openprism", "openprism-mcp"],
      "env": { "OPENPRISM_BACKEND": "opencode" },
      "comment": "Zero-setup launch via uvx (needs `uv` installed) from the published PyPI package. Runs the opencode backend by default — set OPENPRISM_BACKEND=direct + provider keys here for standalone use. Pin a version with `--from openprism==0.1.0`. For local dev: openprism init --host claude-code --local --write"
    }
  }
}
