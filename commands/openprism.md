---
description: Multi-model panel + judge — research synthesis on a hard question
argument-hint: "<hard, open-ended question>"
---

Call the `mcp__openprism__research` tool with `question` set to:

$ARGUMENTS

Then relay the tool's synthesized final answer to me. The judge has already done
the consensus/contradiction/synthesis pass — do not layer your own analysis on
top; just present its answer (you may trim the panel-timing header line).
